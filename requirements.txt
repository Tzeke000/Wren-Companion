# Wren-Companion direct dependencies.
#
# Curated from Ava's brain/ + tools/ + avaagent.py imports. Pinned only
# where version stability matters; rest left unpinned so pip resolver
# can pick compatible versions.
#
# torch + torchaudio are NOT here - installed separately in Step 4 of
# bootstrap.ps1 via the cu128 wheel index (Blackwell sm_120 needs cu128).

# ---- Web / API (operator HTTP server) ----
fastapi>=0.110
uvicorn[standard]>=0.27
httpx
requests
websockets

# ---- Vision ----
opencv-python
opencv-contrib-python
Pillow
numpy

# ---- Face recognition (InsightFace on CUDA) ----
insightface>=0.7
onnxruntime-gpu
onnxruntime
huggingface-hub
soundfile

# ---- Eye tracking + expression detection ----
mediapipe>=0.10,<0.11
# NOTE: protobuf<4 is required by MediaPipe but conflicts with newer
# chromadb/opentelemetry transitively. Bootstrap step 3.5 force-reinstalls
# protobuf==3.20.3 AFTER the rest, which is how Ava's env handles it.

# ---- Audio in (wake + STT) ----
sounddevice
silero-vad
faster-whisper
openwakeword

# ---- Audio out (TTS - Kokoro primary, Piper fallback) ----
kokoro>=0.9
piper-tts>=1.4
pyttsx3

# ---- Memory ----
chromadb
langchain-chroma
langchain-core
langchain-ollama
mem0ai

# ---- LLM-related ----
ollama

# ---- Windows system / computer-use ----
psutil
pywin32
pywinauto
uiautomation
comtypes

# ---- Misc ----
python-dotenv
tenacity
filelock
PyYAML
